# Copy to .env.monitoring at the repository root and chmod 600.
# Configure this as a separate dead-man switch from BACKUP_HEARTBEAT_URL.
OPS_HEARTBEAT_URL=''
DISK_USAGE_LIMIT_PERCENT=80
CERTIFICATE_MIN_DAYS=14
